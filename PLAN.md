# AI 영어 회화 학습 앱 — 상세 기획서

가족 공용(아이+성인) 영어 회화 학습 앱. 발화 중심으로 대화하면서 문법을 자연스럽게 교정받고,
단어·슬랭·구어 표현을 익힌다. 폰/패드에서 마이크·스피커만으로 사용하며 컴퓨터에 연결하지 않는다.

## 0. 확정 사항 요약

| 항목 | 결정 |
|---|---|
| 사용자 | 가족 공용 (아이 + 성인), 프로필 분리 |
| 현재 수준 → 목표 | 초등 3~5학년 수준 → 네이티브 발화자 수준 (슬랭·구어 표현 포함) |
| 대화 시작 | 호출어("영어야!") 자유 대화가 기본, 원하면 버튼으로 주제 선택 |
| AI 비용 | **Claude API 대신 구독(Pro/Max) 활용** — 추가 과금 없음 |
| 서버 | **집에 남는 기기**(노트북/미니PC)에서 24시간 구동 |
| 음성(STT/TTS) | **오픈소스 고품질** 스택을 집 서버에서 직접 구동 — 무료 |
| 월 추가 비용 | **약 0원** (전기료 월 1~3천원 제외) |

---

## 1. 시스템 아키텍처 — "집 서버 올인원"

```
[폰/패드 브라우저 (PWA)]
   마이크 스트리밍 ⇅ HTTPS/WebSocket (Cloudflare Tunnel, 무료)
[집 서버 — 남는 노트북/미니PC, 24시간 구동]
   ├ 앱 서버  : FastAPI (Python) — 정적 PWA 서빙 + 음성 WebSocket
   ├ STT      : faster-whisper (오픈소스 Whisper, 로컬, 무료)
   ├ 대화 LLM : Claude — Agent SDK + 구독 OAuth 토큰 (API 과금 없음)
   ├ TTS      : Kokoro-82M (오픈소스, 상용급 원어민 음성, 로컬, 무료)
   └ DB       : SQLite — 대화 기록·교정 이력·단어장·프로필
```

### 구성 요소 선정 이유

- **프론트엔드 (PWA)**: 앱스토어 없이 폰/패드 브라우저에서 URL만 열면 되고, 홈 화면에
  추가하면 일반 앱처럼 동작. 큰 마이크 상태 표시 + 대화 자막 + 교정 하이라이트 UI.
- **STT — faster-whisper**: OpenAI Whisper의 고속 오픈소스 구현. small/base 모델이면
  CPU에서 실시간 처리 가능하고, 비원어민·아이 발음 인식이 좋다.
- **LLM — Claude (구독 인증)**: Pro/Max 구독 계정에서 `claude setup-token`으로 발급한
  OAuth 토큰을 집 서버의 Claude Agent SDK(Python)에 넣어 인증. API 과금 없이 구독
  사용 한도 내에서 사용. → 상세는 §5.
- **TTS — Kokoro-82M**: Apache 라이선스 오픈소스인데 상용 서비스급으로 자연스러운
  원어민 음성. 경량(82M)이라 GPU 없이 CPU에서 실시간 생성 가능. 발음 학습에 음질이
  중요하므로 이 선택이 핵심. (서버가 라즈베리파이급 저사양이면 Piper로 대체)
- **Cloudflare Tunnel**: 집 서버를 공유기 설정 없이 고정 HTTPS 주소로 외부 공개(무료).
  브라우저가 마이크 권한을 주려면 HTTPS가 필수라서 반드시 필요.
- **SQLite**: 가족용 소규모 데이터라 별도 DB 서버 불필요. 파일 하나로 백업도 간단.

### 서버 기기 요구 사양

- 권장: x86 노트북/미니PC, RAM 8GB 이상 (faster-whisper small + Kokoro 동시 구동 여유)
- 최소: 라즈베리파이 4/5도 가능하나 TTS를 Piper로 낮춰야 함 (음질 하락)
- 노트북이면 덮개 닫아도 꺼지지 않게 전원 설정 + 절전 해제

---

## 2. 핵심 사용 흐름 3가지

### A. 호출어 대화 (기본 모드) — 스마트 스피커처럼

앱을 켜서 거치대에 두면:

1. 브라우저의 VAD(음성 활동 감지)가 상시 대기. Wake Lock API로 화면 꺼짐 방지.
2. **"영어야!"** 감지(호출어는 설정에서 변경 가능 — 예: "Emma!") → 띠링 효과음 +
   AI가 음성 인사로 대화 시작.
3. 이후 **핸즈프리 연속 대화**: 버튼 없이, 말이 끝나면(VAD가 침묵 감지) 자동으로 AI가
   응답. 사용자가 AI 말 중간에 끼어들면 AI 음성 즉시 중단(barge-in).
4. "그만할게" / "Bye Emma" → 세션 종료 + 오늘의 리포트 자동 생성.

> **정직한 제약 안내**: 브라우저 보안 정책상 앱이 화면에 떠 있어야 마이크를 쓸 수 있다.
> 화면이 꺼지거나 다른 앱으로 전환하면 호출어가 동작하지 않는다. 따라서 "시리"처럼
> 아무 때나 부르는 방식이 아니라, **아침에 한 번 켜서 거치대에 두는 스마트 스피커**
> 사용 패턴이 현실적인 목표다.

### B. 자유 대화 (주제 없이 이런저런 이야기)

- AI가 시간대·요일·지난 대화 기록을 바탕으로 스몰토크를 먼저 시작
  ("Good morning! Did you sleep well?" / "How was school today?")
- 사용자가 꺼내는 화제를 따라가고, 대화가 늘어지면 AI가 자연스럽게 새 화제 제안
- 지난 세션에서 배운 단어·표현을 슬쩍 재등장시켜 복습 (사용자는 복습인지 모르게)

### C. 주제 선택 대화 (앱 버튼)

- **주제 카드**: 일상 / 학교 / 여행 / 음식 / 게임 / 영화 / 운동 ...
- **롤플레이 카드**: 가게 점원, 공항 직원, 식당 주문, 병원, 길 묻기, 친구와 화해하기 ...
- **"오늘의 슬랭으로 대화하기"**: 그날의 구어 표현 2~3개를 정해두고 대화 속에서 익히기

---

## 3. 교육 설계

### 가족 프로필

- 첫 화면에서 사용자 선택 (아이1 / 아이2 / 엄마 / 아빠)
- 프로필별로 레벨·단어장·리포트·AI 말투가 분리 저장
- 아이 프로필은 연령 적합 표현만 노출 (비속어 계열 슬랭 자동 필터링)

### 레벨 로드맵 (초등 3~5 → 네이티브)

| 단계 | 목표 | AI의 말하기 방식 |
|---|---|---|
| 1. 기초 (현재) | 짧은 문장, 기초 시제, 일상 어휘 | 짧고 쉬운 문장, 또박또박, 자주 되물어주기 |
| 2. 중급 | 복문, 시제 혼용, 관용구 입문 | 보통 속도, 관용구 소량 섞기 |
| 3. 상급 | 구어체·슬랭·뉘앙스, 자연스러운 리듬 | 네이티브 속도, 슬랭·리덕션 적극 사용 |

AI가 최근 발화 데이터(문장 길이, 오류율, 어휘 다양성)를 보고 레벨 업을 제안한다.

### 문법 교정 — Recast 기법

- 대화 중에는 절대 지적하지 않고 **올바른 문장으로 자연스럽게 되받기만** 한다.
  - 예: "I go to park yesterday" → "Oh, you **went to the park** yesterday? What did you do there?"
- 교정 내역은 Claude 응답에서 구조화(JSON)로 병행 추출해 DB에 조용히 저장
  → 세션 종료 후 리포트에서 확인 (내 문장 → 올바른 문장 → 짧은 이유)

### 슬랭·구어 표현 학습

1. AI가 대화에 구어체를 자연스럽게 섞어 쓰고, 처음 나온 표현은 짧게 풀어준다
   ("It's a piece of cake! — 참, piece of cake는 식은 죽 먹기란 뜻이야")
2. 세션당 "오늘의 표현" 2~3개 목표
3. 표현 노트에 자동 저장, 다음 대화에서 **사용자가 직접 써보도록** AI가 상황을 만들어 유도
4. 아이/성인 프로필별 수위 구분 (아이: cool, hang out, no way / 성인: 폭넓게)

### 단어장 + 간격 반복 복습

- 모르는 단어를 물으면 쉬운 영어 + 한국어로 설명하고 단어장에 자동 저장
- 저장된 단어·표현은 망각 곡선 주기(1일 → 3일 → 7일 → 14일)로 다음 대화에 재등장
- 단어장 화면에서 듣고 따라 말하기 퀴즈 제공

### 세션 리포트

- 오늘 교정된 문법 목록 (내 문장 → 올바른 문장 → 이유)
- 새로 배운 단어·표현
- 발화량 통계 (말한 문장 수, 대화 시간)
- 연속 학습일(streak), 프로필별 누적 그래프

---

## 4. 대화 엔진: 파이프라인 방식과 업그레이드 전략

### 두 방식이란

- **파이프라인**: 음성→텍스트(STT) → Claude가 텍스트로 응답 → 텍스트→음성(TTS).
  3단계가 분리된 구조. **이 프로젝트의 1차 채택 방식.**
- **실시간(speech-to-speech)**: OpenAI Realtime API, Gemini Live처럼 음성이 통째로
  들어가서 음성이 통째로 나오는 단일 모델.

### 파이프라인이 "학습 기능 제어"에 유리한 이유

1. **모든 단계에서 텍스트를 소유한다** — 사용자의 정확한 발화 원문과 AI 응답이 전부
   텍스트로 남는다. 교정 추출, 단어장 자동 저장, 세션 리포트, 간격 반복 복습이 모두
   이 텍스트 위에서 동작한다. 실시간 방식은 음성이 곧바로 음성이 되므로 이 데이터를
   얻기 어렵거나 부정확하다.
2. **프롬프트 완전 제어** — Recast 규칙, 프로필별 레벨·수위, 오늘의 슬랭 주입, 복습
   단어 재등장 같은 교육 로직을 시스템 프롬프트로 정밀하게 조정할 수 있다.
3. **부품 교체 자유** — STT/LLM/TTS를 각각 더 좋은 것으로 독립적으로 교체 가능.
4. **Claude 구독으로 구동 가능** — 실시간 음성 API는 Claude에 없어서 구독 활용 불가.

파이프라인의 단점: 응답 지연 2~4초, 억양·감정 정보가 텍스트로 바뀌며 소실,
말 끊기(barge-in)를 직접 구현해야 함.

### 방식 비교

| | 파이프라인 (채택) | 실시간 speech-to-speech |
|---|---|---|
| 응답 지연 | 2~4초 (최적화 시 1.5~2초) | 1초 미만 |
| 말 끊기 | 직접 구현 (가능) | 기본 지원, 매우 자연스러움 |
| 억양·감정 | 소실 (텍스트 경유) | 사용자 억양에 반응 |
| 교정·학습 데이터 | **완전 확보** | 부차적 트랜스크립트에 의존 |
| 프롬프트 제어 | **정밀** | 약함 |
| Claude 구독 | **사용 가능 → 0원** | 불가 (OpenAI 등 별도 과금) |
| 비용 | 0원 | 음성 토큰 과금 — 하루 30분이면 월 수만~십수만 원 |

### 구체적 업그레이드 방안 (권장 순서)

**0단계 — 파이프라인 안에서 지연 단축 (비용 0원, 가장 먼저)**
- Claude 응답을 스트리밍으로 받아 **문장 단위로 즉시 TTS** → 첫 문장이 1.5~2초 안에
  스피커로 나오기 시작
- VAD 기반 barge-in: 사용자가 말을 시작하면 AI 음성 즉시 중단
- 대부분의 "실시간 같은 느낌"은 이 단계로 충분히 확보 가능

**1단계 — 하이브리드 (추천 업그레이드 경로)**
- 음성 대화 루프만 실시간 API(OpenAI Realtime 등)로 교체
- Claude는 백그라운드 **"학습 분석기"**로 유지: 실시간 API가 남기는 대화 트랜스크립트를
  Claude가 받아 교정 추출·리포트·단어장을 계속 생성
- 결과: 1초 미만 지연 + 학습 기능 그대로 유지
- 추가 비용: 실시간 API 사용료 (이때부터 유료)

**2단계 — 완전 전환 (미래)**
- 실시간 모델이 교정·학습 데이터 추출까지 충분히 잘하게 되면 전체 교체

**업그레이드가 쉬운 이유 (설계 원칙)**
- 코드를 "대화 엔진 인터페이스"로 추상화한다:
  `음성 입력 → { 응답 음성, 대화 텍스트, 교정 목록 }`
- DB·리포트·단어장·복습 로직은 이 인터페이스의 출력만 소비한다. 따라서 엔진 내부를
  파이프라인 → 실시간으로 갈아끼워도 **학습 기능 코드는 한 줄도 수정할 필요가 없다.**

---

## 5. Claude 구독 활용 — 방법과 리스크

### 방법

1. Pro/Max 구독 계정으로 집 서버에 Claude Code 설치 후 `claude setup-token` 실행
   → 장기 OAuth 토큰 발급
2. 집 서버의 Claude Agent SDK(Python)가 이 토큰으로 인증
3. API 과금 없이 구독 사용 한도(5시간 롤링 윈도우) 내에서 대화 생성

### 공식 지원 현황 (2026-07 기준)

- Anthropic은 개인 용도의 Agent SDK 구독 사용을 지원한다 (Pro/Max 요금제의
  통상적 개인 사용 범위). 사업·프로덕션 용도는 API 키가 공식 경로.
- 2026년 6월 "Agent SDK 월간 크레딧" 제도를 예고했다가 **시행 보류** — 당분간
  기존처럼 구독으로 사용 가능하며, 변경 시 사전 공지 예정.

### 리스크 대비

- 대화 엔진의 LLM 호출부를 한 곳(모듈 하나)에 모아둔다.
- 정책이 바뀌면 API 키 방식(Haiku 4.5 — $1/$5 per MTok, 대화 용도로 충분한 성능,
  가족 사용량 기준 월 5천~1만원 수준)으로 **한 줄 전환** 가능하게 설계한다.

---

## 6. 비용 요약

| 항목 | 비용 |
|---|---|
| Claude 대화 | 기존 구독료에 포함 (추가 0원) |
| STT (faster-whisper) | 오픈소스 로컬 구동 (0원) |
| TTS (Kokoro-82M) | 오픈소스 로컬 구동 (0원) |
| 서버 | 집 남는 기기 (전기료 월 1~3천원) |
| 외부 접속 (Cloudflare Tunnel) | 0원 |
| **월 추가 비용 합계** | **약 0원 (전기료 제외)** |

참고: 나중에 하이브리드(실시간 API) 업그레이드를 선택하면 그때부터 사용량 기반 과금 발생.

---

## 7. 개발 로드맵

### 1단계 — 음성 대화 루프 MVP (1~2주)
- [ ] 집 서버 셋업: Python 환경, FastAPI, faster-whisper, Kokoro
- [ ] Claude Agent SDK + 구독 토큰(`claude setup-token`) 인증 연결
- [ ] Cloudflare Tunnel로 HTTPS 외부 접속 구성
- [ ] PWA 프론트: 누르고 말하기 버튼 → STT → Claude → TTS 재생, 자막 표시
- [ ] Recast 교정 시스템 프롬프트 v1 (레벨: 초등 3~5 기준)
- **완료 기준: 폰에서 AI와 영어로 3분 이상 자연스러운 대화 성공**

### 2단계 — 핸즈프리 + 호출어 (1주)
- [ ] 브라우저 VAD(음성 활동 감지)로 버튼 없는 연속 대화
- [ ] "영어야!" 호출어 감지 + 호출어 이름 설정
- [ ] Wake Lock(화면 유지), barge-in(말 끊기)
- [ ] Claude 스트리밍 + 문장 단위 TTS로 체감 지연 1.5~2초 달성

### 3단계 — 학습 기능 (2주)
- [ ] 가족 프로필 (프로필별 레벨·데이터 분리, 아이 프로필 표현 필터)
- [ ] 교정 내역 JSON 추출 → SQLite 저장 → 세션 리포트 화면
- [ ] 단어장 / 슬랭·표현 노트 + 자동 저장
- [ ] 주제 카드 / 롤플레이 카드 / 오늘의 슬랭 모드

### 4단계 — 고도화 (지속)
- [ ] 간격 반복 복습 (배운 단어·표현을 대화에 자동 재등장)
- [ ] 슬랭·구어 표현 커리큘럼 (주제별 표현 세트)
- [ ] 학습 통계 대시보드 (프로필별, 부모용 아이 리포트)
- [ ] 레벨 자동 승급 제안
- [ ] (원하면) 하이브리드 실시간 업그레이드 — §4의 1단계 경로

---

## 8. 시작 전 준비물 체크리스트

- [ ] 집에 24시간 켜둘 기기 확보 (노트북/미니PC, RAM 8GB 권장)
- [ ] Claude Pro/Max 구독 계정 (이미 있음)
- [ ] Cloudflare 계정 (무료 가입)
- [ ] 폰/패드 브라우저: Android Chrome 또는 iOS Safari 최신 버전
