# AI 영어 회화 학습 앱 — 기획 및 개발 계획

## 1. 목표

집에서 핸드폰/패드만으로, 마이크와 스피커를 통해 AI와 **영어로 대화**하면서:

- **발화 중심 학습**: 듣고 말하는 것이 학습의 중심
- **자연스러운 문법 교정**: AI가 틀린 문장을 지적하듯 고치는 게 아니라, 대화 흐름 속에서 올바른 문장으로 되받아 말해줌 (언어교육에서 검증된 **Recast 기법**)
- **자연스러운 단어 암기**: 대화 중 나온 새 단어를 자동으로 기록하고, 다음 대화에서 다시 등장시켜 반복 노출

컴퓨터 연결 없이 **폰/패드 브라우저에서 URL만 열면 되는 웹앱(PWA)** 형태로 만든다.
앱스토어 등록이 필요 없고, 홈 화면에 추가하면 일반 앱처럼 쓸 수 있다.

## 2. 무엇을 만드나 — 핵심 사용 흐름

1. 폰/패드에서 웹앱을 연다 → 오늘의 주제 선택 (일상, 학교, 여행, 자유대화 등)
2. 마이크 버튼을 누르고(또는 자동 감지) 영어로 말한다
3. AI가 음성으로 대답한다
   - 문법이 틀렸으면 **대답 속에 올바른 표현을 자연스럽게 녹여서** 말해줌
   - 예: 학습자 "I go to park yesterday" → AI "Oh, you **went to the park** yesterday? What did you do there?"
4. 대화가 끝나면 **세션 리포트** 자동 생성:
   - 오늘 고쳐진 문법 포인트 목록 (내 문장 → 교정 문장)
   - 새로 나온 단어장 (뜻, 예문 포함)
5. 다음 세션에서 AI가 지난 단어/문법을 대화에 슬쩍 다시 끼워넣어 복습시킴 (간격 반복)

## 3. 시스템 구성 (아키텍처)

```
[폰/패드 브라우저 (PWA)]
   마이크 녹음 ──▶ [서버 API]
                     1) STT: 음성 → 텍스트 (Whisper 등)
                     2) LLM: 대화 + 교정 (Claude)
                     3) TTS: 텍스트 → 음성
   스피커 재생 ◀── 음성 응답 + 화면에 자막/교정 표시
                     4) DB: 대화 기록, 단어장, 교정 이력 저장
```

- **프론트엔드**: Next.js 기반 모바일 웹 (PWA). 큰 마이크 버튼 하나가 중심인 단순한 UI
- **STT (음성→텍스트)**: OpenAI Whisper API (비원어민 발음 인식이 좋음)
- **대화 엔진 (LLM)**: Claude API — 시스템 프롬프트로 "친절한 원어민 선생님 + Recast 교정 + 학습자 레벨 맞춤" 역할 지정. 교정 내역을 구조화된 형태(JSON)로 함께 뽑아 세션 리포트에 사용
- **TTS (텍스트→음성)**: OpenAI TTS 또는 ElevenLabs (자연스러운 원어민 음성)
- **DB**: Supabase(무료 티어) — 대화 기록, 단어장, 학습 통계
- **배포**: Vercel(무료 티어) — 배포하면 URL이 생기고, 폰에서 그 URL만 열면 됨. 집 컴퓨터를 켜둘 필요 없음

### 대안: 실시간 음성 API
OpenAI Realtime API(speech-to-speech)를 쓰면 지연이 더 짧고 끊고 말하기(barge-in)가 자연스럽다.
다만 교정 내역 추출·학습 데이터 축적 등 "교육 기능" 제어는 위의 STT→LLM→TTS 파이프라인이 훨씬 유리하므로, **1차는 파이프라인 방식으로 만들고, 나중에 실시간 API로 업그레이드**하는 것을 추천.

## 4. 학습 설계 (교육적 핵심)

| 요소 | 구현 방법 |
|---|---|
| 발화 유도 | AI가 항상 열린 질문으로 끝맺어 학습자가 계속 말하게 함. 학습자 발화가 짧으면 "Tell me more!" 식으로 확장 유도 |
| 문법 교정 (Recast) | 대화 중에는 지적하지 않고 올바른 문장으로 되받기만 함. 교정 내역은 뒤에서 조용히 기록 → 세션 후 리포트로 확인 |
| 단어 암기 | 학습자가 모르는 단어를 물으면 쉬운 영어+한국어로 설명하고 단어장에 자동 저장. 이후 세션에서 재등장시켜 자연 복습 |
| 레벨 조절 | 초급(짧고 쉬운 문장, 천천히)/중급/고급을 설정. AI가 학습자 발화 수준을 보고 난이도 자동 제안 |
| 동기 부여 | 연속 학습일(streak), 오늘 말한 문장 수, 누적 교정 그래프 |

## 5. 단계별 개발 로드맵

### 0단계 — 바로 체험 (개발 전, 오늘부터 가능)
ChatGPT 앱의 음성 모드에 "영어 선생님 역할 + 틀리면 자연스럽게 고쳐 말해줘" 프롬프트를 주고 써보면서, 어떤 기능이 꼭 필요한지 감을 잡는다. (커스텀 앱이 필요한 이유 = 교정 기록, 단어장, 복습, 아이 수준 고정 등)

### 1단계 — MVP: 말하고 듣는 대화 루프 (약 1~2주)
- [ ] Next.js 프로젝트 + 모바일 UI (마이크 버튼, 대화 자막)
- [ ] 녹음 → Whisper STT → Claude 응답 → TTS 재생 파이프라인
- [ ] Recast 교정 시스템 프롬프트 + 레벨 설정 (초/중/고급)
- [ ] Vercel 배포 → 폰/패드에서 접속 확인
- **완료 기준: 폰에서 AI와 영어로 자연스럽게 3분 이상 대화가 가능하다**

### 2단계 — 학습 기능 (약 2주)
- [ ] Supabase 연동: 대화/교정/단어 저장
- [ ] 세션 종료 리포트 (교정 목록, 새 단어장)
- [ ] 단어장 화면 + 간단한 복습 퀴즈 (단어 듣고 말하기)
- [ ] 주제 카드 (일상/학교/여행/롤플레이: 가게 점원, 공항 등)

### 3단계 — 고도화 (이후 지속)
- [ ] 지난 학습 내용을 다음 대화에 자동 반영 (간격 반복 복습)
- [ ] 학습 통계 대시보드 (아이가 쓴다면 부모용 리포트)
- [ ] 실시간 음성 API 전환 (지연 단축, 자연스러운 말 끊기)
- [ ] 발음 피드백 (STT 신뢰도 기반 or 발음 평가 API)

## 6. 예상 비용 (개인 사용 기준)

- Vercel / Supabase: 무료 티어로 충분
- API 사용료: 하루 20~30분 대화 기준 대략 **월 1~3만원 수준**
  (Whisper STT + Claude + TTS 합산. TTS를 브라우저 내장 음성으로 대체하면 더 절감 가능하나 음성 품질은 떨어짐)

## 7. 다음 단계

이 계획에 대한 확인이 필요한 사항:

1. **학습자가 누구인가?** (아이/성인, 현재 영어 수준) → 프롬프트·UI 난이도 설계에 반영
2. **1단계 MVP부터 바로 개발 시작할지?** → 승인하면 이 리포지토리에 Next.js 프로젝트를 만들어 진행
3. API 키 준비: OpenAI(STT/TTS), Anthropic(대화) 계정 및 키 발급 필요
